name: Migrate ctype header

on:
  push:
    branches:
      - feat/ctype-header-c11

permissions:
  contents: write

jobs:
  migrate:
    if: github.actor != 'github-actions[bot]'
    runs-on: ubuntu-24.04
    steps:
      - uses: actions/checkout@v4
        with:
          ref: feat/ctype-header-c11
          fetch-depth: 0
      - name: Apply ctype header support
        run: python3 tools/migrate_ctype_header.py
      - name: Commit migrated sources
        run: |
          if git diff --quiet -- preprocess_v2.c Makefile test/ctype_header.sh; then
            echo 'No migration changes required.'
            exit 0
          fi
          git config user.name github-actions[bot]
          git config user.email 41898282+github-actions[bot]@users.noreply.github.com
          git add preprocess_v2.c Makefile test/ctype_header.sh
          git commit -m 'Support C11 ctype.h'
          git push origin HEAD:feat/ctype-header-c11
