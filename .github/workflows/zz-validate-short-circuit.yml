name: Validate preprocessor short-circuit fix

on:
  push:
    branches:
      - fix-pp-if-short-circuit

permissions:
  contents: read

jobs:
  test:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: Full test suite
        run: make test
