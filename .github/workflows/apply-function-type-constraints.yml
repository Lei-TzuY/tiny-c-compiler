name: Apply function type constraints
on:
  push:
    branches:
      - agent/function-type-constraints
permissions:
  contents: write
jobs:
  migrate:
    if: github.actor != 'github-actions[bot]'
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
        with:
          fetch-depth: 0
      - name: Apply migration
        run: python3 tools/apply_function_type_constraints.py
      - name: Test
        run: make clean && make test
      - name: Commit permanent changes
        run: |
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git add parse.c Makefile README.md test/function_type_constraints.sh
          git diff --cached --quiet && exit 0
          git commit -m "Enforce function type constraints"
          git push
